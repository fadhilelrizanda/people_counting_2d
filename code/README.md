# Code Directory

This directory contains the core source code for the local CPU execution of the People Counting system. 

## Contents
* Preprocessing utilities
* Local tracking and detection implementations (`tracker.py`, `detector.py`, etc.)
* Inference pipeline entry points

This code is executed locally to process video feeds using optimized ONNX models and relies on `requirements.txt`.
